#include <stdio.h>
#include <stdlib.h>

// 5. Escreva um programa que imprima todos os numeros inteiros de 200 a 100 (em ordem decrescente).

int main() {
	
    for (int i = 200; i >= 100; i--) {
        printf("%d\n", i);
    }
    
    return 0;
}
